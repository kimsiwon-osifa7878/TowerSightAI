# INTENT.md — 대화 의도와 작업 방식 기록

이 문서는 저장소 소유자(kimsiwon)와 에이전트 사이에 축적된 **의도, 합의된 작업 방식, 의사결정 이유,
현장 이력**을 기록한다. 새 세션이 이전 대화 컨텍스트 없이도 같은 방식으로 작업을 이어가기 위한
문서다. 코드/아키텍처 사실은 `CLAUDE.md`, 안전·규칙은 `AGENTS.md`, 사용법은 `README.md`가 담당하고,
여기는 "왜 그렇게 했고, 어떻게 일하기로 했는가"만 담는다.

## 1. 프로젝트가 지금 어디에 있는가

- 주차기 AI 안전감시 시스템의 **현장 시운전 단계**. 개발기에서 만든 것을 현장 장비에 배포하며
  실환경 문제(NAS 인증, 카메라 세션, Hailo 하드웨어)를 하나씩 해결하는 중이다.
- 최종 PLC OK는 여전히 설계상 차단 상태이며, 이는 버그가 아니라 안전 전제조건(캘리브레이션,
  실제 PLC, 스테이지 AI)이 미구현이기 때문이다.
- 근래 작업 흐름: NAS 연결 확인 → 카메라 폴백 → 운영 콘솔 전면 개편(시안 B) → 추론 자동 전환 →
  메뉴 이름 정리 → Hailo 상태 모니터. 다음 항목은 `PLAN.md`와 아래 §5.

## 2. 운영 환경 (두 대의 장비)

| | 개발기 | 현장기 |
|---|---|---|
| 호스트/계정 | `erumtni@erumtni-NucBox-G3` | `erumtni2@pakrio-shinantower` |
| 하드웨어 | (개발용) | **GMKtec GMK-G3 (Intel N100 미니PC)** + Hailo-8 M.2 |
| 역할 | 개발·검증 (실카메라 2대 + Hailo-8 연결됨) | 실제 주차기 설치 현장 (원격 관리 — 물리 접근 제한적) |
| 카메라 IP | front .244, 좌측면 .193 (우측면 .191 미접속) | front .235, 좌측면 .239, 우측면 .211 |
| ceiling | placeholder (BIRDVIEW_MODE=disabled) | placeholder — .239를 복제해 둔 상태(§5 참고) |
| Hailo M.2 링크 | 정상 | **불안정 — Gen3 가능인데 Gen1로 강등, 유휴 중 행 재발(§5)** |

- 두 장비 모두: 프로젝트 `.venv`(Python 3.12) + Hailo Apps venv(`~/hailo-apps/venv_hailo_apps`),
  리소스는 `/usr/local/hailo/resources`, HailoRT 4.23.0 + TAPPAS Core 5.1.0.
- 두 장비 모두 pip `opencv-python`이라 **GStreamer 백엔드 없음** → 카메라 프리뷰는 전 카메라
  FFmpeg 폴백으로 동작한다 (의도된 상태).
- NAS: Synology SFTP (`erumtni.synology.me:45222`, `/home/shinhantower`). 새 장비는 반드시
  `~/.ssh/known_hosts`에 호스트 키를 등록해야 한다 (sftp로 1회 접속, 지문 대조).

## 3. 합의된 작업 방식 (이대로 진행할 것)

- **커밋·푸시는 사용자가 지시할 때만.** 커밋 메시지는 사용자가 불러주는 짧은 한국어 제목 그대로
  (예: "NAS 연결 확인 추가"). main 직push.
- **실기 검증이 기본.** UI 변경은 pytest만으로 끝내지 않는다: 실제 GUI를 띄워 버튼을 클릭하고
  스크린샷(`tmp/operator-ui-verification/`)과 로그로 확인한 뒤 보고한다.
  `tools/verify_operator_ui_screenshot.sh`가 기준 도구.
- **현장 문제는 로그로 재현→원인 확정→수정→재검증 순서.** 추측으로 고치지 않는다. 사용자가
  현장 로그를 붙여넣으면, 원인 후보를 좁히는 진단 명령을 단계적으로 제시하고 출력 해석을 돌려준다.
- **문제 해결 후에는 재발 방지까지.** 예: 오류 메시지 개선, 감시 항목 추가, 문서/체크리스트 반영.
- 문서는 코드와 함께 갱신한다 (README, CLAUDE.md, docs/implementation, DESIGN.md).
- 시각 작업은 **HTML 시안 → 사용자 승인 → PyQt 이식** 순서. 승인된 시안은 `docs/design/`에 남긴다.
- 사용자 응대는 한국어. 코드 주석/문서는 기존 관례(영어) 유지, UI 문자열은 한국어.
- 사용자가 재확인 요청 시 기존 방향을 재검토하되, 안전 규칙(AGENTS.md)은 타협하지 않는다.

## 4. 주요 의사결정과 이유 (뒤집을 때는 근거를 확인할 것)

- **운영자 모드 = 개발자 콘솔** (사용자 요청). 사이드바는 섹션(운영/진단/시스템) 네비게이션 전용,
  실행 버튼은 각 페이지 안에. 시각 언어는 승인된 **시안 B「패널 HMI」** (앰버 액센트, 계기식 카메라
  타일) — `docs/design/operator-console-proposals.html`. 드라이버(사용자) 화면의 시안/네이비 언어는
  건드리지 않는다 (DESIGN.md).
- **메뉴 이름은 직관 우선** (사용자 요청): 차량 감지/사람 감지/번호판 인식/레이더 (LD2410)/실행 로그/
  프로그램 종료 등. 버튼은 "…시작/중지" 문법.
- **추론 자동 전환**: 다른 AI 실행 중 새 AI를 누르면 거부하지 않고 기존 중지→대기열→자동 시작
  (사용자 요청). `_pending_user_purpose_task_id` 재사용.
- **카메라 프리뷰 FFmpeg 폴백을 전 카메라로 확장**: pip cv2에 GStreamer가 없어 front만 나오던 현장
  문제의 해결. 회전은 소프트웨어 처리.
- **증거 레코더는 stream2** (`CAMERA_N_RECORD_RTSP_URL`): Tapo stream1 동시 세션 한도(2)와
  프리뷰+추론이 충돌하므로. 비워두면 레코더가 stream1로 폴백해 추론이 RTSP 400으로 죽는다.
- **PurposeInferenceRunner는 일시적 자식 사망(RTSP 400 등)을 3회까지 재시작**하되, Hailo 치명
  오류(드라이버/장치)와 정상 종료(이미지 LPR)는 재시작하지 않는다.
- **Hailo 상태 모니터**(60초 주기): 현장 칩 행 사고의 신호(제어 응답 실패, AER RxErr 증가)를 그대로
  감시. 상태줄 HAILO 알약 + 시스템 점검 패널 + `towersightai.hailo.health` 로그. 진단 전용 —
  안전 게이트에 영향 없음.
- **운영자 진입**: 현장에서 2초 홀드가 불편 → 우하단 `운영자 모드` 버튼 추가(임시 서비스 컨트롤).
  숨김 핫스팟과 Ctrl+Shift+O는 유지.
- **NAS 연결 확인**: 단순 ping이 아니라 실제 쓰기 경로(원자적 업로드+SHA-256 재검증)를
  `connectiontest/`에 수행. 카메라 수신 중이면 2초 클립 동봉.
- 테스트 검증 스크립트가 띄운 UI가 안 죽고 세션을 점유해 추론 400을 유발한 적 있음 → verify
  스크립트에 SIGTERM 10초 후 kill -9 폴백. **추론이 이유 없이 400이면 잔존 UI 프로세스부터 의심.**
- **2026-09-09 개발 장비 추론 전멸 원인 = 고아 Hailo 자식**: 9/2 17:41에 뜬 `process_monitoring` 자식이
  부모 UI 사망 후 systemd 밑에 7일간 살아 `/dev/hailo0`를 점유 → 이후 모든 추론이
  `HAILO_OUT_OF_PHYSICAL_DEVICES(74)`. `fuser /dev/hailo0`로 찾음(프로세스명이 `Hailo Multisource App`으로
  바뀌어 pgrep 패턴에 안 걸림). 대책: 자식에 PR_SET_PDEATHSIG+ppid 감시, 상태 모니터에 점유 프로세스 표시,
  치명 메시지에 점유 PID, 시스템 점검의 `고아 프로세스 종료` 버튼. 자식 env에서 레거시
  GST_PLUGIN_PATH/LD_LIBRARY_PATH 제거도 추가(셸 export가 남아 있으면 assertion으로 무이벤트).
- **2026-09-10 2차: 고아 증거 녹화기 + RTSP 400 폭주**. 죽은 UI(사용자 1회, 검증 스크립트 2회)가 남긴
  `event_video_recorder.py` 5개가 카메라 RTSP 세션을 계속 쥐어 Tapo 세션 한도로 감시 자식이 400으로
  죽고, 1초 재시도가 세션을 또 소모해 스스로 폭주(`hailo 프로세스가 없는데 계속 재설정`의 정체).
  대책: 녹화기에도 PDEATHSIG+ppid 감시+정지 5초 강제 종료, 400 재시도 10초 대기, 감시 자동 시작은
  카메라 집합 4초 안정 후 1회 실행 + 실패 시 30/60/120초 백오프, 상태 모니터가 고아 녹화기도 표시.
  **UI가 비정상 종료되면 `ss -tn | grep :554`와 `fuser /dev/hailo0`부터 확인.**
- **2026-09-10 레이더 상시 raw 기록**(`docs/implementation/radar-raw-logging.md` 명세 구현): 1 Hz `ld2410_sample`
  + 레이더 감지 창(`radar_window_started/closed`, 3초 확인·5초 해제) + 창 증거(`radar`/`radar_end` 스냅샷,
  30초 상한 클립, 60초 스로틀). **분석 전용, 엔진 입력 아님**. (2026-09-16 `observe_radar` 자체를 제거 — 아래 항목 참고)
- **2026-09-10 운영자 추론 정리(사용자 지시)**: `이전 AI Detection`과 일반 멀티스트림 경로를 UI에서 제거.
  수동 추론은 `사람 감지`·`차량 감지` 둘뿐이며 `전체 카메라`에도 같은 버튼을 둠(누르면 실행 중 추론 중지 →
  클릭한 추론 시작). `프로세스 감시`(사람+차량 동시, 엔진 입력, 자동 시작)는 **유지하고 박스도 그대로 표시**
  — 사용자가 문답으로 확정. 전체 카메라의 사람+차량 박스는 이 감시 추론에서 나온다.
- **프로세스 재정의(2026-09-02, 현장 실측 반영)**: IDLE 사람 감시는 버드뷰+전면+좌측면(우측면은
  문이 열리면 바깥이 보여 제외), 진입 트리거는 우측면 차량 감지(기본 conf 0.6 × 연속 5프레임,
  운영자 설정), 번호판은 전면 1초 주기 + 차량진입선(상단, 이 선 아래 = 진입) 아래만 유효 + 다수결. **출고 단계는 사용자가
  명시적으로 제거** — PLC 출고 신호가 생기기 전까지 미고려. PLC 미연결 구간은 타이머 가정
  (무인 10초 → OK 전송(모의), 작동 60초 → IDLE) — 모두 FakePLCAdapter 뒤, payload simulated=True.
- **운영자 튜닝값은 `.env`가 아니라 `data/operator-settings.json`** (감시 설정 페이지에서 편집,
  저장 즉시 엔진 반영, 자식 프로세스 재시작 불필요 — 자식은 conf 0.2 고정, 필터링은 부모).
- **감시 추론은 process_monitoring 단일 자식** (사람+차량 라벨 동시): Tapo 세션 한도 때문에 Hailo
  자식 2개는 불가. 수동 테스트 태스크 시작 시 자동 전환으로 감시가 멈추고, 끝나면 자동 재개(10초 쿨다운).
- 경고는 화면+소리(QtMultimedia, 없으면 무음 강등). 정렬은 당분간 전면 사다리꼴(원근) 바퀴 유도선 + bbox 정지
  판정 휴리스틱 — 3D 외곽박스는 아래 확정안대로 구현 예정(PLAN.md).
- **3D 차량 직육면체 확정안(2026-09-09, 문답으로 확정)**:
  - 목표: 카메라 3(`rear_side`, 차량 **앞면+좌측면** 사선)과 카메라 4(`opposite_side`, **뒷면+우측면**
    사선)로 차량 직육면체(길이·폭·높이 + 팔레트 기준 위치·회전각)를 실시간 추정. 카메라-모서리 대응은
    `.env`의 `VEHICLE_BOX_FRONT_LEFT_CAMERA`/`VEHICLE_BOX_REAR_RIGHT_CAMERA`로 바꿀 수 있음.
  - **전면 카메라(카메라 2, `front`)도 쓸 수 있으면 쓴다(2026-09-09 추가)**: 정면에서 보므로 앞면의
    좌우 폭·가운데선(좌우 치우침·요 각)·범퍼 하단 높이를 사선 카메라보다 직접적으로 잡을 수 있고,
    이미 번호판·바퀴 유도선에 쓰는 프리뷰 프레임을 그대로 사용(추가 RTSP 세션 없음). 역할은
    **보조·교차 검증**: 전면이 없거나 NG여도 3·4만으로 상자를 만들 수 있어야 하고, 전면이 있으면
    폭·좌우 오프셋의 불확실도를 줄인다. 세 카메라 결과가 어긋나면 큰 쪽 불확실도를 채택(보수적).
  - 용도: ① 정렬 안내(좌우/전후) ② 정차 위치 판정(허용 범위) ③ 차량 크기 제한 검사. 한도·허용 오차의
    기본값은 승인도, 튜닝은 나중에 운영자 설정. 결과는 안전 게이트의 **입력**일 뿐이며 불확실하면 WAIT/NG.
  - 정확도 목표: **주행 중 포함 ±5cm** — 설계 목표이지 보장 아님(C310 롤링 셔터·블러, 공중에 뜬 범퍼 점).
    개발 장비의 1:18 모형+팔레트 사각형으로 먼저 실측하고 달성 여부를 숫자로 보고.
  - 방법: Hailo bbox는 **ROI로만**(사용자 판단: bbox가 튀어 정밀 입력으로 부적합). 외곽은 OpenCV —
    배경차분(IDLE 배경 자동 갱신)과 에지 방식 둘 다 만들어 스냅샷으로 비교 후 선택. 기하는
    타이어 접지선→바닥 평면 고정→옆면 수직 평면→앞·뒷면 수직 모서리→상자 완성, 두 카메라 교차 검증,
    프레임 간 칼만 필터. 추론 추가 없음, N100 CPU 10fps 목표.
  - 캘리브레이션: 렌즈 내부 파라미터는 개발 장비 C310으로 1회 측정(같은 기종). 현장 외부 파라미터는
    팔레트 네 귀퉁이 + 레일 두 줄 실치수(마커 설치 불가). 운영자 점 찍기 UI 필요, 미검토 캘리브레이션은
    최종 OK 차단.
  - **체커보드 촬영은 화면의 상자로 지시한다(2026-09-16, 현장 피드백)**: 사용자가 실제로 찍어 보니
    ① 보드가 제자리에 없는데도 바로 찍히고 ② "왼쪽/오른쪽"이 카메라 기준인지 사람 기준인지 헷갈렸다.
    그래서 자세 지시를 전부 **A4 가로 비율 사각 상자**로 바꿨다. 상자 안에 보드가 들어오고 **3초를
    유지해야** 촬영되며, 벗어나면 대기 시간이 초기화되고 이유를 한국어로 알려 준다. 기울임 자세는
    좌우/위아래 **어느 방향이든** 인정한다(마주보는 변의 길이 차이로 판정). 자세는 15개 → 13개.
    `지금 촬영`은 상자 조건을 일부러 건너뛴다(예외 상황용).
  - **지면 기준점은 운영자가 찍는다(2026-09-16, 사용자 지시 "1을 만들어")**: 팔레트 데크는 **긴 변만**
    노란 테두리라 끝단(x=±2675)에 자동으로 잡을 특징이 없다(한쪽은 주황 스토퍼 프레임이라는 3차원
    구조물, 반대쪽은 열려 있음). 추측으로 채운 것이 1차 랩 교정이 틀어진 원인이었다. 그래서 운영자
    콘솔에 `지면 기준점` 페이지를 두고 **팔레트 네 모서리 + 주황 스토퍼 밑동** 다섯 번 클릭으로
    외부 파라미터를 잡는다. 스토퍼는 "어느 쪽이 진입구인가"를 말로 하지 않고 정하는 랜드마크다
    (체커보드 상자와 같은 원칙 — 좌/우 표현을 쓰지 않는다). 모서리 클릭 순서는 상관없고, 네 가지
    대응을 모두 풀어 되맞춤이 가장 좋은 것을 고른다. 결과는 팔레트 기준 카메라 위치(x,y,z)와
    방향(팬·틸트·롤), 되맞춤 오차를 한국어 점검표로 보여주고, 투영한 팔레트와 500 mm 격자를 화면에
    겹쳐 눈으로 확인하게 한다. **측정 파일일 뿐 캘리브레이션 유효 표시가 아니다.**
  - **캘리브레이션 결과 확인 수단을 둔다(2026-09-16, 사용자 요청)**: 잘 됐는지 알 방법이 없었다.
    `결과 확인` 버튼이 ① 항목별 한국어 점검표(재투영 오차·최악의 장·수평 화각·샘플 수·가장자리 자세
    수·기울임 자세 수)와 등급, ② **왜곡 보정 전/후 비교 이미지**(양쪽에 똑같은 곧은 노란 격자를 겹침 —
    보정이 잘 됐으면 실제 직선이 격자와 나란해진다)를 보여준다. 저장된 측정 파일을 다시 읽어 쓰므로
    재촬영 없이도 확인할 수 있다. **확인 전용이며 캘리브레이션을 유효로 만들지 않는다.**
  - 좌표계: **팔레트 사각형 중심 원점**, x=팔레트 길이(진입 방향), y=폭, z=위. 단위 mm.
  - 기준 치수 출처: `refers/구로신안타워스테카_0_승인도_20260210_내부높이5500mm.pdf` J001(수용차량제원·
    주차구획 상세) / J002(입고층 평면도). 주차구획 5,350×2,200, 레일 내폭 2,106, 구획 높이 1,600/1,900,
    설계기준 L5205×W2000(미러 2100)×H1550/1850, 바퀴폭 2,000 이하. 기본값은 `VehicleEnvelopeConfig`에
    내장, `.env`의 `VEHICLE_BOX_*`로 덮어씀. J002에는 턴테이블 Ø6100·컨베이어 배치만 있고 팔레트
    자체 치수 표기는 없어 J001의 주차구획을 팔레트 사각형으로 사용.
  - **지면 교정은 턴테이블 원판으로 한다(2026-09-16, 사용자 지시)**: 체커보드 보정 전이라도 카메라
    위치가 고정이고 바닥 원판 Ø6,100이 승인도에 있으므로 그것으로 축척을 잡는다. 실제로는 원판 +
    노란 레일(내폭 2,106) + 팔레트 사각형 조합이 가장 잘 맞았고, 전면 카메라는 알려진 x 기준이
    팔레트 끝 하나뿐이라 **레일 인터로킹 매트 톱니 주기(114.8 mm, 측면 카메라 교정에서 역산)**로
    x 축척을 세웠다. 검증 코드는 `vehicle_box_test/`.
  - **검증 산출물 형식(2026-09-16, 사용자 지시)**: 판정 결과를 **이미지 안에** 새긴다. 성공이면 바닥
    사각형·앞뒤 끝·좌우 바퀴선과 치수를, 실패면 **왜 안 됐는지를 한국어로** 이미지 한쪽에 적어
    **이미지만 보고 판단**할 수 있게 한다. 전체는 로컬 HTML 보고서로 묶는다.

- **데이터 분석 대시보드(2026-09-10, 사용자 지시)**: 레이더(LD2410) vs 카메라 사람 감지 정확도 비교용
  `towersightai/analyze/` + `towersightai-analyze`. **개발·검증 전용, 로컬 실행만** — 현장 장비에서 운영하지 않는다.
  NAS 주소는 현장이 늘어날 것을 대비해 `data/analysis/sites.json`에 현장별로 등록(대시보드 설정 페이지/CLI).
  읽기 전용, 안전 모듈 import 금지(테스트 고정). 사람이 스냅샷·클립을 보고 에피소드마다 있음/없음/불가를 표시해
  정밀도·상호 재현율을 낸다. 레이더 raw 보강(`docs/implementation/radar-raw-logging.md`)은 별도 창에서 진행.

- **레이더는 검증 전용(2026-09-16, 사용자 지시)**: LD2410은 카메라 감지와의 정확도 비교를 위한 기록용일 뿐이다.
  **주차기 실제 작동과 안전에는 사용하지 않는다.** 프로세스 엔진의 `observe_radar`와 add-only 사람 신호를 제거했고,
  사용자 모드·안전 게이트·PLC 경로 어디에도 레이더가 들어가지 않는다. 사람 존재 판정은 카메라만으로 한다.
  레이더 값은 raw 데이터(`ld2410_sample`, `radar_window_*`, `radar_sample`)와 증거 미디어로만 남는다.

- **번호판 인식 실패 원인과 수정(2026-09-16)**: 현장 95분 데이터에서 진입 9건 중 1건만 인식됐다. 원인은 둘로,
  ① 전면에 이미 정차한 차가 있으면 `_front_vehicle_stable`이 1~2회 판독 만에 투표를 끝냈고(5건),
  ② 우측면 트리거가 차가 전면에 오기 전에 걸려 30초 타임아웃이 먼저 지났다(3건, 차는 21~82초 뒤 도착).
  FastALPR 자체는 문제가 아니다 — 차가 전면에 있던 유일한 건은 2회 판독으로 신뢰도 0.996에 성공했다.
  수정: 판독 시간을 **전면 카메라가 차를 처음 본 시점**부터 재고, 차가 안 오면 `arrival_timeout_seconds`로 종료하며,
  정차 단축은 판독이 하나라도 있거나 `min_read_seconds` 경과 후에만 적용한다. 세 값 모두 감시 설정 페이지에서 조정.

- **증거 파일명은 한국시간 + `_kr`(2026-09-16, 사용자 지시)**: 폴더 날짜는 한국시간인데 파일명만 UTC라
  15:46 촬영분이 `064634`로 보였고 같은 날짜 폴더 안에서 이름 정렬이 시간순도 아니었다. 이제
  `20260916-154634-702684_kr-...` 형태로 날짜·시각을 모두 넣고 시간대를 표시한다. 기존 업로드분은 UTC 이름 유지.

- **입고/출고 판별(2026-09-16, 사용자 지시)**: 현장에서 출고 때도 `진입 준비`가 뜨고 주차 안내까지 가던 문제.
  원인은 출고 판단 로직이 아예 없어 우측면 트리거가 걸리면 무조건 진입 사이클을 시작한 것. 확정안:
  ① 트리거 후 바로 진입으로 가지 않고 `entry_classify`(공개 상태는 IDLE)에서 방향을 먼저 판별한다.
  ② 번호판이 읽히면 입고. 전면 bbox가 좁고 서 있으면(폭 0.54, 비 1.57 수준) bbox만으로도 입고로 진행.
  ③ 전면 bbox가 화면을 가득 채우며 옆으로 누우면(폭 0.92~1.00, 비 3.3~4.4) 출고. 화면엔 `출고중`만 띄우고
     사람이 감지돼도 문제로 보지 않는다. ④ 번호판도 없고 형태도 불확실하면 **에러**이며 진입 확정을 하지 않는다.
  노란 표시선 기준은 쓰지 않기로 했다(사용자 지시). 출고는 `vehicle_exit_started/ended`로 따로 기록해
  입고 통계와 번호판 인식률이 오염되지 않게 한다.

- **번호판은 뒷자리 4자리만 신뢰(2026-09-17, 사용자 지시)**: OCR 모델(`cct-xs-v2-global-model`)에 한글이 없어
  가운데 글자를 라틴 문자로 바꾸고 그 과정에서 앞뒤 숫자까지 밀린다(213가9135 → `2137I913`, 1757 → `757`).
  해결: 번호판 상자에서 오른쪽만 다섯 지점으로 잘라 재판독하고, 여백을 넓힌 재판독과 전체 텍스트 뒷자리까지
  후보로 모아 **다수결**한다. 현장 8건 실측에서 단일 방식은 6/8인데 다수결은 8/8. 4자리가 안 나오면 추측하지 않고
  거부한다(`no_tail_digits`). 전체 문자열은 `plate_text`로 감사용만 남긴다. 추가 비용 약 105 ms(1초 주기의 22%).

## 5. 미해결 항목 (다음 세션이 이어받을 것)

즉시(현장):
- [ ] **Hailo 칩 하드웨어 행 — 물리 링크 문제로 확정 (재발 중)**. 장비: GMKtec GMK-G3(N100).
  증상: 노드(`/dev/hailo0`)·드라이버(4.23.0)·lspci 다 정상인데 칩이 제어 요청에 무응답
  (`HAILO_DRIVER_OPERATION_FAILED(36)`, open `/dev/hailo0` **error 5=EIO**, `identify`가 `[-]`).
  근본 원인: **PCIe 링크 불안정**. `LnkCap 8GT/s(Gen3) x2`인데 `LnkSta 2.5GT/s(Gen1) x2`로 **3단계
  강등** + 상위 포트(0000:00:1d.0) **AER RxErr 누적(8/31 11건 → 9/2 12건)**, 재열거 복구 후 **약
  47분 만에 재발**. 온도 정상, ASPM은 `not supported`라 무관 → 소프트웨어/전원관리/속도 문제 아님.
  **속도 하향(Gen2)은 레버 아님**(이미 Gen1인데도 에러).
  - 복구 전 반드시 증거 저장: `~/hailo-hang-<ts>/` 번들(00~09), 특히 `01-pcie-aer.txt`(RxErr·LnkSta)는
    복구하면 초기화됨. 판독은 `ls -d ~/hailo-hang-*/ | tail -1` 후 [2]RxErr/[3]LnkSta/[5]identify.
  - **원격 복구(물리 접근 없이)**: ① PCIe 재열거 —
    `pkill -f operator_ui; pkill -f hailortcli` 후
    `sudo modprobe -r hailo_pci; echo 1|sudo tee /sys/bus/pci/devices/0000:02:00.0/remove; sleep 2;
    echo 1|sudo tee /sys/bus/pci/rescan; sudo modprobe hailo_pci; hailortcli fw-control identify`
    (이전에 통함, 임시). ② `reboot` — **9/2 재발 때 reboot만으로 회복됨**(identify 정상, LnkSta가
    Gen1→**8GT/s Gen3** 복귀; width `x2 (downgraded)`는 슬롯이 원래 x2 배선이라 정상). warm reboot로도
    링크가 다시 트레이닝될 수 있음이 확인됨 — 원격에서 우선 시도할 값어치 있음.
  - **근본 해결(현장 방문 시, 여전히 필요)**: reboot는 그때그때 복구일 뿐 링크가 한 번 Gen1까지
    무너진 이력이 있으므로 물리 대책이 근본임. 완전 전원 차단(코드 분리 30초) → **M.2 재장착 + 접점
    청소**, 중간 라이저/연장 케이블 있으면 재체결·교체(RxErr 1순위 용의자). 그래도 재발 → 다른 M.2
    슬롯/모듈 교체.
  - **재발 조기경보 = RxErr 추세**. reboot/재열거로 AER 카운터가 0으로 초기화되니, 이후
    `cat /sys/bus/pci/devices/0000:00:1d.0/aer_dev_correctable | grep RxErr` 와 `LnkSta`(8GT/s 유지
    여부)를 주기적으로 확인. RxErr가 다시 오르거나 LnkSta가 2.5GT/s로 떨어지면 곧 행 → 현장 방문 예약.
  - health 모니터(`towersightai.hailo.health`)는 이 상태를 정확히 error로 잡고 있음(소프트웨어는 정상).
- [ ] **Hailo 행 2차 유형(2026-09-16 14:51)**: 전조 없이 버스에서 단절. dmesg `Device disconnected while
  opening device` 반복, LnkSta는 8 GT/s 유지, RxErr 0, 온도 67.9°C 정상 → RxErr 추세로는 예고 안 됨.
  재열거(modprobe -r → remove → rescan → modprobe)로 즉시 복구됨(재부팅 불필요). 물리 대책은 동일:
  전원 완전 차단 후 M.2 재장착·접점 청소·라이저 재체결. 재발 간격이 짧아지면 슬롯 변경/모듈 교체.
- [ ] 현장 `.env`에 `CAMERA_N_RECORD_RTSP_URL` 4줄(stream2) 추가 — 안내는 완료, 적용 확인 필요.
- [ ] 현장 `CAMERA_1_RTSP_URL`(ceiling placeholder)이 좌측면 실IP(.239) 복제 상태 — 접속 불가
  주소(`192.0.2.10`)로 교체 권고했으나 미확인.
- [ ] 현장에서 차량/사람 감지 실추론 성공 확인 (하드웨어 복구 후).
- [ ] 프로세스 엔진 현장 튜닝: 우측면 트리거 임계값/연속 프레임(문 밖 통행 오탐), 차량진입선 y,
  바퀴 유도선 위치, 표시 장치 스피커 확인 — 모두 감시 설정 페이지에서.
- [ ] NAS 업로드 방식(즉시/예약) 현장 대역폭 확인 후 결정.

중기(코드) — `PLAN.md`·`CLAUDE.md` §11과 동일 방향:
- [ ] 안전 게이트를 UI 모델 프로퍼티에서 단일 서비스로 중앙화.
- [ ] **3D 차량 직육면체 — 1차 검증 완료(2026-09-16), 다음은 왜곡 보정**. 검증 랩은
  `vehicle_box_test/`(본체와 분리, 상세는 그 폴더의 `README.md`·`CONTEXT.md`).
  사용자 지시로 **"검증 → 그 다음 본체 반영"** 순서를 지킨다. **사용자 모드에는 사용자가 지시할
  때까지 반영하지 않는다.**
  - 1차에서 확인된 것: NAS 표본 자동 수집(차량이 담긴 미디어만 선별), **체커보드 없이 원판
    Ø6100·레일 2106·팔레트 5350×2200만으로 지면 교정 성립**(front 재투영 오차 13 mm),
    Hailo 없이 CPU 배경차분으로 차량 실루엣 추출, 사선 카메라에서 **바닥 사각형·앞뒤 끝·바퀴선**
    산출(이미지 317장 중 판정 성공 18장). 판정 실패는 **사유를 이미지 안에 한국어로** 새긴다.
  - **2차(2026-09-16, 체커보드 측정 후): 직육면체를 그리게 됐다.** 우측면 카메라를 새 촬영
    방식으로 측정(RMS 0.252 px, good)해 **전 카메라에 적용**(같은 C310이라 나머지는 빌려 씀).
    왜곡 보정 → 측정된 K로 solvePnP → 레일로 자세 다듬기 → **호모그래피를 자세에서 생성**.
    자세-지면 불일치 16 % → 잔차 34 mm, 높이까지 나온 장 0 → 6장(1,163~1,488 mm).
    부수 발견: **레일 톱니 주기가 114.8 mm가 아니라 ~344 mm**였다(왜곡 영상에서 부주기를
    잡은 것) — 1차의 전면 카메라 기하가 3배 틀려 있었고, "전면은 지면을 1.5 m만 본다"는
    1차 결론도 이 오류의 산물이었다. 차량 없는 프레임에 상자가 그려지던 오검출은 실루엣
    '채움' 기준(25 %)으로 막았다. 상세는 `vehicle_box_test/CONTEXT.md` §7.
  - 1차에서 막혔던 것(원인 규명 완료, 2차에서 ①③ 해소): ① `opposite_side`는 자세 복원이 지면과 **16 % 어긋나
    직육면체를 못 그렸다 → 체커보드 내부 파라미터 측정이 실제로 필요함이 수치로 확인**
    ② **전면 카메라는 지면이 진입부 1.5 m만 보여 주차 완료 차량의 접지선을 못 본다**(전면은
    번호판·좌우 치우침 보조, 길이·위치는 사선 카메라 몫) ③ **증거 클립(stream2)과 스냅샷(stream1)의
    화각이 달라** 같은 교정을 못 쓴다(stream2 전용 교정 필요) ④ 사선 한 대로는 폭이 부정확
    ⑤ `rear_side` 미교정.
  - 다음 순서: `rear_side` 지면 교정(두 사선 교차 → 폭·요각·높이) → 카메라별 체커보드 실측
    (지금은 우측면 값을 빌려 씀) → 팔레트 먼 쪽 기준점을 운영자 점 찍기 UI로 정식 입력
    → 접지선 개선(그림자·록커패널) → 정확도 수치화(±5 cm 목표 대비)
    → 그 뒤에 `towersightai/`에 `vehicle_box` 스테이지 신설.
  - 현장 사진·영상은 `data/field-media/{front,rear_side,opposite_side}/`(카메라별 폴더, 파일명 접두사
    불필요, git 미추적)에 모은다. 랩이 NAS에서 직접 받은 자료는 `vehicle_box_test/data/`(git 미추적,
    **번호판이 보이므로 외부 공유 금지**).
- [ ] 스테이지 AI(정렬/번호판 처리/사람·장애물 융합/차내 탑승자), 캘리브레이션 UI, 실제 PLC 어댑터.
- [ ] GStreamer 지원 OpenCV로 교체(정비 시) — 폴백은 임시 우회.

## 6. 이 문서의 유지

작업 방식이 바뀌거나 큰 의사결정이 추가되면 이 파일을 갱신한다. §5는 완료 시 체크하고 오래된
항목은 제거한다. 이 문서와 코드가 어긋나면 코드·`CLAUDE.md`가 우선이며, 어긋남 자체를 여기 기록한다.
